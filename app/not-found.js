import NotFoundContent from "@/app/components/NotFoundContent";

/**
 * `notFound()` 호출 시 · 레이아웃 안에서 렌더
 * (루트 레이아웃의 Header·Footer 포함)
 */
export default function NotFound() {
  return <NotFoundContent />;
}
